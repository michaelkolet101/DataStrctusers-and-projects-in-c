void push()
{
	/* Check whether queue is Full – Check
	 ((rear == SIZE-1 && front == 0) || (rear == front-1)). */
	 
	 /* If it is full then display Queue is full. If queue is not full then, check if (rear == SIZE – 1 && front != 0) if it is true then set rear=0 and insert element */
}

void pop()
{
	/* Check whether queue is Empty means check (front==-1) */
	
	/* If it is empty then display Queue is empty. If queue is not empty then 
																	next step */
																	
	/* Check if (front==rear) if it is true then set front=rear= -1 else check
									 if (front==size-1), if it is true then set
											  front=0 and return the element. */
}

